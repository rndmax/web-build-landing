{
  "type": "module",
  "scripts": {
    "test": "node tests/quiz.test.mjs && node tests/faq.test.mjs && node tests/content.test.mjs && node tests/repository.test.mjs",
    "lint": "node scripts/lint.mjs",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "check": "npm run format:check && npm run lint && npm test"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
